{{>header}}

<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3 pt-4 pb-4">
    	<div class="todo-error rounded p-4" style="background-color:#fff;">
    		<div class="todo-view-action mb-3">
      			<h4 class="d-inline align-middle">Something Wrong!</h4>
      			<a href="{{backurl}}" class="btn btn-info btn-sm float-right" title="Trash"><i class="fa fa-arrow-left"></i> back</a>
      		</div>
      		<span class="bg-danger d-block p-2 rounded">{{error}}</span>
      	</div>
    </div>
  </div>
</div>

{{>footer}}